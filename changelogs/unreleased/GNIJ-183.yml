changes:
- type: changed #changed, removed, added
  component: checkbox
  description: aria-checked attribute added
- type: changed #changed, removed, added
  component: multiple-upload
  description: aria-labelledby added to the input element using the button
