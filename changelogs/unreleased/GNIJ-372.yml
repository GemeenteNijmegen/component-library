versionBump: patch # patch, minor, major
changes:
  - type: changed #changed, removed, added
    component: checkbox
    description: Removed the aria-checked attribute since it is not needed for a native checkbox
    what: [JS, HTML] # what has changed 1 or more of: [HTML, CSS, JS]
