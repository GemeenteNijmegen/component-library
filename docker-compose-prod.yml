version: '3.3'
services:
  componentlibrary:
    build:
      context: .
      dockerfile: Dockerfile-prod
    image: ${CI_REGISTRY_IMAGE}:${DOCKER_TAG}
    ports:
      - "8083:80"
    deploy:
      replicas: 2
      update_config:
        parallelism: 1
        delay: 5s
        monitor: 2s

